1) Getting the assignment opened from GitHub
   - Forked the provided repository into my own GitHub account
   - Cloned the forked repository onto my local machine
   - Used "code ." to open the project in VS Code

2) Running the starter tic-tac-toe code
   - Ran ./helper init to install dependencies
   - Ran ./helper dev to start the development server
   - Opened the app in the browser and confirmed the provided tic-tac-toe code worked before modifying it

3) Understanding the provided code
   - Examined how the code was split into Square, Board, Game, and calculateWinner
   - Traced how props were passed down and how the board was represented as a 1D array of length 9
   - Looked at how currentMove % 2 === 0 was used to determine the current player

4) Working with the Square and Board components
   - Kept the reusable Square structure from the starter code
   - Studied how Board mapped each board position to a fixed array index
   - Used that structure as the basis for later changes

5) Understanding and modifying click logic
   - Looked at how the original handleClick function rejected occupied squares, copied the board with squares.slice(), and placed the current player’s symbol
   - Used that logic as the starting point for later modifications
   - In some versions, separated the click logic into helper functions to reduce nesting and make it easier to test

6) Understanding state and winner checking
   - Traced how history and currentMove were used to manage board states
   - Reviewed how calculateWinner checked the predefined row, column, and diagonal combinations
   - Kept the same winner-checking structure since Chorus Lapilli still uses three in a row

7) Testing and debugging
   - Tested the program after each major change
   - At one point only X was appearing, which I traced to the move-count / turn logic not updating correctly
   - After fixing that, turns alternated correctly and the game behaved as expected

8) CSS and final verification
   - Increased square size and status text size to improve readability
   - Verified that the board rendered correctly, turns alternated properly, winners were detected, and extra moves were blocked after a win

9) General observations
   - The app would not run unless ./helper dev was active and fully started
   - Browser and terminal error messages helped identify syntax and logic errors during development